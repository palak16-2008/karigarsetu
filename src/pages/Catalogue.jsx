import { Printer } from "lucide-react";
import { getProducts } from "../services/api";
export default function Catalogue(){const ps=getProducts().slice(0,6);return <div className="workspace catalogue-page"><div className="catalogue-toolbar"><div><span className="eyebrow">DIGITAL CATALOGUE</span><h1>Your handcrafted collection.</h1></div><button className="btn btn-primary" onClick={()=>window.print()}><Printer size={17}/> Print / Save as PDF</button></div><div className="catalogue-sheet"><div className="catalogue-header"><span className="eyebrow">KARIGARSETU COLLECTION</span><h2>Handcrafted with purpose.</h2><p>A digital collection by Indian artisans.</p></div><div className="catalogue-grid">{ps.map(p=><div className="catalogue-item" key={p.id}><img src={p.image} alt={p.name}/><span>{p.craft}</span><h3>{p.name}</h3><p>{p.description}</p><strong>₹{p.price.toLocaleString("en-IN")}</strong></div>)}</div></div></div>}